@model IEnumerable<dynamic>

<div class="container mt-4">

    <h3 class="fw-bold mb-4">
        <i class="fa-solid fa-spinner loader"></i> 
        Student Progress</h3>

    <div class="table-responsive">
        <table class="table table-bordered table-hover">

            <thead class="table-dark">
                <tr>
                    <th>Student</th>
                    <th>Project</th>
                    <th>Task</th>
                    <th>Status</th>
                    <th>Created</th>
                    <th>Due</th>
                    <th>Weekly Report</th>
                    <th>Action</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td style="word-break: break-all; width:15%">@item.StudentName</td>
                        <td style="word-break: break-all; width:20%">@item.ProjectTitle</td>
                        <td style="word-break: break-all; width:20%">@item.TaskTitle</td>

                        <td>
                            <span class="badge
                                    @(item.Status == "Completed" ? "bg-success" :
                                                                    item.Status == "In Progress" ? "bg-warning" : "bg-secondary")">
                                @if(item.Status == "Completed")
                                {
                                    <i class="fa-solid fa-check-double"></i>
								}
	
                            @item.Status
                        </span>
                    </td>

                        <td>@Convert.ToDateTime(item.CreatedAt).ToString("dd MMM yyyy")</td>

                        <td>
                            @(item.DueDate != null ? Convert.ToDateTime(item.DueDate).ToString("dd MMM yyyy") : "N/A")
                        </td>

                        <td>
                            <a href="/Mentor/WeeklyReports?studentId=@item.StudentId"
                               class="btn btn-sm btn-info">
                                View Report
                            </a>
                        </td>

                        <td>
                            <a href="/Mentor/Reply?studentId=@item.StudentId"
                               class="btn btn-sm btn-primary">
                                Reply
                            </a>
                        </td>

                    </tr>
                                }
            </tbody>

        </table>
    </div>

</div>